# Randomuser.me demo of React with parcel-bundler

## Usage:

```shell
npm install
npm run start
```

## Behavior

Visit the site on the localhost URL in the console, or visit the hosted version using the URL above.
